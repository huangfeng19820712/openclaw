// Global variables for Control UI

interface Window {
  // Auto-pair script: initial token from pairing flow
  __OPENCLAW_INITIAL_TOKEN__?: string;

  // Control UI base path (injected by server)
  __OPENCLAW_CONTROL_UI_BASE_PATH__?: string;
}
